# Copy to .env and fill in. NEVER commit .env.
# OpenAI-compatible API used for semantic analysis (S4+).
COMPASS_API_KEY=
COMPASS_API_BASE_URL=
# Optional: session cookie for TU Delft SuccessFactors collector (S5+).
TUDELFT_COOKIE=
